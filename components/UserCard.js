import { View, Text, StyleSheet } from "react-native";

const UserCard = () => {
  return (
    <View>
      <Text>User card</Text>
    </View>
  );
};

export default UserCard;

const styles = StyleSheet.create({});
